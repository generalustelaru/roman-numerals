## Roman <-> Decimal Converter CLI mini-app in NodeJS.

I wrote this to see how placing most decisions into data objects would influence how the code looks.

NodeJS is a prerequisite.

Run `node start.js` to run the CLI app.